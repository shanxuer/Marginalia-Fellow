# Vault Template Rules

This folder stores the active Markdown templates, the template rule configuration, and human-readable governance notes for the Obsidian research vault.

`template-rules.json` is the executable source of truth for folder-to-template validation. New governance documents should be add-only and should not replace existing rules unless the user explicitly permits modification.
